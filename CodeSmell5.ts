// ANTES

numberIsEven(num: number): boolean {
    return (num % 2 == 0 ? true : false);
    return false;
}


// DESPUÉS

numberIsEven(num: number): boolean {
    return (num % 2 == 0 ? true : false);
}